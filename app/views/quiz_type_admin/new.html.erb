<div class="wrapper wrapper-content animated fadeInDown">
  <div class="row">
    <div class="col-md-6 col-md-offset-3">

      <div class="ibox">
        <div class="ibox-title">
          <% if @quiz_type.blank? %>
            <h5>Registrar Tipo de Evaluación Teórica</h5>
          <% else %>
            <h5>Editar Tipo de Evaluación Teórica</h5>
          <% end %>
        </div>
        <div class="ibox-content">
          <div class="row">
            <div class="col-md-12">
              <%= form_for :quiz_type, html: {role: "form"} do |f| %>
                <div class="form-group">
                  <label><span class="text-danger">*</span>Nombre</label>
                  <%= f.text_field :nombre, class:"form-control", required:"true" %>
                </div>
                <br>
                <%= f.submit "Enviar", class: "btn btn-primary" %>
                <%= link_to quiz_type_admin_path do %>
                  <span class="btn btn-danger">Cancelar</span>
                <% end %>
            <% end %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>